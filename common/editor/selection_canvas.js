function SelectionCanvas() {

    //constant
    var NUM_OBJECTS = 5;

    //fields
    var canvas = document.getElementById('selectionCanvas');
    var ctx = canvas.getContent();
    var width = canvas.width;
    var height = canvas.height;
    var objects = [];

    (function() {
        addBlock();
        addEnemy();
    })();


    //private helpers
    function addBlock() {

    }

    function addEnemy() {

    }

    function getLocation(index) {

    }

}